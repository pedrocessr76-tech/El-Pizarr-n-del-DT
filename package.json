{
  "name": "el-pizarron-del-dt",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"cd apps/server && npm run start\" \"cd apps/client && npm run dev\"",
    "dev:server": "cd apps/server && npm run start",
    "dev:client": "cd apps/client && npm run dev",
    "build": "npm run build --workspace=server && npm run build --workspace=client"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "swagger-ui-express": "^5.0.1"
  }
}
