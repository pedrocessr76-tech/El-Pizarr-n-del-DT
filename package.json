{
  "name": "el-pizarron-del-dt",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"cd apps/server && npm run start\" \"npm run dev --workspace=client\"",
    "dev:server": "cd apps/server && npm run start",
    "dev:client": "npm run dev --workspace=client",
    "build": "npm run build --workspace=server && npm run build --workspace=client"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "swagger-ui-express": "^5.0.1"
  }
}
